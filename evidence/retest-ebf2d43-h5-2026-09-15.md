# Retest actualizado de H-5 y W-6

**Commit:** `ebf2d43a79259c7c337be661bdbc2410465d36a4`  
**Estado probado:** clon aislado del commit remoto; Krypta original solo se leyó.

## Cambios revisados

- Se añadió una prueba de H-5 con claves X25519 reales: quien posee la privada de una identidad y el PeerID del contacto reconstruye el mismo `S` y puede fabricar un sobre válido para el buzón ciego. El buzón por PeerID sigue dependiendo de que el nodo sea honrado para no falsear el remitente.
- Se corrigieron tres bytes NUL accidentales en `CallService.kt` y `ChatService.kt`; la comprobación independiente no encontró NUL en archivos versionados Kotlin/Go/Markdown.
- W-6 permanece documentado como limitación de disponibilidad; no hubo cambios de producción que lo eliminen.

## Ejecución

`RatchetTest` y `ChatServiceTest` dirigidos terminaron `BUILD SUCCESSFUL` (44 tareas, 0 fallos). La suite completa del puente Go con `-race` ya había pasado en el estado inmediatamente anterior y no hay cambios Go funcionales en este commit.

## Evaluación

H-5 queda mejor demostrado, no corregido. El resultado confirma que la autenticación al nivel de `S` permite KCI y que la etiqueta del buzón no añade una identidad independiente. W-6 sigue abierto; la ayuda nueva describe correctamente su recuperación manual. El cambio de NUL es una corrección de calidad del código, no criptográfica.
